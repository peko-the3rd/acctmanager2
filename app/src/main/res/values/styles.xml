<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.RaisedButton.Accent" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorPrimary</item>
    </style>
    <style name="UpsertButton" parent="Theme.AppCompat">
        <!--item name="colorControlHighlight">@color/indigo</item-->
        <item name="android:textStyle">bold</item>
        <!--item name="android:textColor">@color/white</item-->
        <item name="colorButtonNormal">@color/green</item>
    </style>
    <style name="deleteButton" parent="Theme.AppCompat">
        <!--item name="colorControlHighlight">@color/indigo</item-->
        <item name="android:textStyle">bold</item>
        <!--item name="android:textColor">@color/white</item-->
        <item name="colorButtonNormal">@color/deep_orange</item>
    </style>
    <style name="finishButton" parent="Theme.AppCompat">
        <!--item name="colorControlHighlight">@color/indigo</item-->
        <item name="android:textStyle">bold</item>
        <item name="colorButtonNormal">@color/blue_grey</item>
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/indigo</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/green</item>
        <!-- 省略 -->
    </style>
    <style name="AppThemeDarkActionBar" parent="AppTheme.Base">    <!-- AppTheme.Baseを継承 -->
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <!-- item name="android:windowEnterTransition">@android:transition/explode</item-->
        <!-- item name="android:windowExitTransition">@android:transition/explode</item-->
        <!-- item name="android:windowSharedElementEnterTransition">@android:transition/explode</item-->
        <!-- item name="android:windowSharedElementExitTransition">@android:transition/explode</item-->
        <!-- item name="android:activityCloseEnterAnimation">@android:transition/explode</item-->
        <!-- item name="android:activityCloseExitAnimation">@android:transition/explode</item-->

    </style>
</resources>
